# 两数之和 Todo List

## 1. 理论复习
- [ ] **理解题目要求**：
  - 确保完全理解题目要求和限制条件。
  - 理解输出是数组的索引，而不是数字本身。
  - 理解题目保证只有一个解。

- [ ] **复习暴力解法**：
  - 理解两层循环的逻辑。
  - 理解为什么内层循环从 `i + 1` 开始。
  - 理解时间复杂度 \(O(n^2)\) 和空间复杂度 \(O(1)\)。

- [ ] **复习哈希表解法**：
  - 理解哈希表的使用方法。
  - 理解如何通过一次遍历解决问题。
  - 理解时间复杂度 \(O(n)\) 和空间复杂度 \(O(n)\)。

## 2. 代码实现
### 暴力解法
```javascript
function twoSum(nums, target) {
    for (let i = 0; i < nums.length; i++) {
        for (let j = i + 1; j < nums.length; j++) {
            if (nums[i] + nums[j] === target) {
                return [i, j];
            }
        }
    }
    throw new Error("No two sum solution");
}
```

### 哈希表解法
```javascript
function twoSum(nums, target) {
    const map = new Map();
    for (let i = 0; i < nums.length; i++) {
        const complement = target - nums[i];
        if (map.has(complement)) {
            return [map.get(complement), i];
        }
        map.set(nums[i], i);
    }
    throw new Error("No two sum solution");
}
```

## 3. 测试代码
- [ ] **测试示例**：
  - 使用题目给出的示例数据测试代码。
  - 确保代码能够正确输出结果。

- [ ] **测试边界情况**：
  - 测试数组中只有两个元素的情况。
  - 测试数组中有重复数字的情况。
  - 测试数组中包含负数或零的情况。
  - 测试数组长度接近上限（如 10,000）的情况。

### 测试用例
```javascript
console.log(twoSum([2, 7, 11, 15], 9)); // 输出：[0, 1]
console.log(twoSum([3, 2, 4], 6));      // 输出：[1, 2]
console.log(twoSum([3, 3], 6));         // 输出：[0, 1]
console.log(twoSum([-1, -2, -3, -4], -7)); // 输出：[2, 3]
console.log(twoSum([1000000000, -1000000000], 0)); // 输出：[0, 1]
```

## 4. 优化和改进
- [ ] **优化代码**：
  - 确保代码的可读性和逻辑清晰。
  - 检查是否有更简洁的写法。

- [ ] **性能优化**：
  - 确保哈希表解法的时间复杂度为 \(O(n)\)。
  - 确保代码在大数据量下运行效率高。

## 5. 模拟面试
- [ ] **模拟面试练习**：
  - 找一个朋友或同学模拟面试场景。
  - 在白板或纸上手写代码，解释每一步的逻辑。
  - 练习如何清晰地表达思路。

- [ ] **回答常见问题**：
  - 准备回答面试官可能提出的问题，例如：
    - “为什么选择哈希表解法而不是暴力解法？”
    - “如何优化哈希表解法？”
    - “如果数组中有多个解，你会怎么做？”

## 6. 复习和总结
- [ ] **复习代码**：
  - 在面试前复习代码，确保能够快速写出正确的代码。
  - 确保理解每一步的逻辑。

- [ ] **总结经验**：
  - 总结在练习过程中遇到的问题和解决方案。
  - 总结如何在面试中快速找到最优解。
